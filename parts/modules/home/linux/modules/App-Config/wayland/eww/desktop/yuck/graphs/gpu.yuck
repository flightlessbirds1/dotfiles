(defvar gpu 0)

(defwidget gpu []
  (box
    :class "graphBox"
    :orientation "vertical"
    :tooltip "${gpu}%"
    :width 60
    (graph    
      :class "gpuGraph"
      :value gpu
      :time-range "30s"
    )
    (label
      :class "gpuText"
      :text "gpu"
    )
  )
)
